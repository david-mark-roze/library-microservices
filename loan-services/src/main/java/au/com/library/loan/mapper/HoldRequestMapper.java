package au.com.library.loan.mapper;

import au.com.library.loan.dto.HoldRequestDTO;
import au.com.library.loan.entity.HoldRequest;
import au.com.library.shared.mapper.SimpleMapper;
import org.mapstruct.Mapper;

/**
 * Mapper interface for converting between {@link HoldRequest} entities and {@link HoldRequestDTO} data transfer objects.
 * This interface extends {@link SimpleMapper} to provide basic mapping functionality, and is annotated with {@link Mapper} to indicate that it is a MapStruct mapper component.
 * The @Mapper annotation specifies that this mapper should be registered as a Spring component, allowing it to be injected and used throughout the application where needed.
 * The mapping logic will be automatically generated by MapStruct at compile time, based on the method signatures defined in this interface and the structure of the source and target classes.
 * This mapper is used to facilitate the conversion of hold request entities to a format suitable for API responses, ensuring that the necessary data is included while abstracting away any internal details of the entity.
 *
 * @see HoldRequest
 * @see HoldRequestDTO
 * @see SimpleMapper
 */
@Mapper(componentModel = "spring")
public interface HoldRequestMapper extends SimpleMapper<HoldRequest, HoldRequestDTO> {
}
